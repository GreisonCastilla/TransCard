import { ref } from "vue";

export const text = ref("")
export const auxId = ref("xxxxxxxxxx")
export const balance = ref("↑ Consulta tu saldo ↑")
export const cards = ref([])
export const popup = ref(false)
